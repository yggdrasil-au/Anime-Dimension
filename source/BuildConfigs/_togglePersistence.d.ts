/**
 * Toggle Persistence System Type Definitions (Build Config - Astro)
 * Provides build-time utilities for Astro components.
 */

/**
 * Generates a stable page ID from the current pathname.
 */
export function generatePageId(pathname: string): string;
